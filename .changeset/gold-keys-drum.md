---
'mastra': patch
'create-mastra': patch
'@internal/playground': patch
---

Fixed Studio workflow run pages so active runs without persisted snapshots show an explanation instead of trying to inspect an empty stream.
